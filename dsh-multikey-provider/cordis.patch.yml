# The official provider remains active and installed. The package contributes
# only its own pool routes. The Models client is replaced at composition level
# because rc.6 exposes no provider-card extension slot.

- id: ui-settings-models
  name: '@deepseek-ai/dsh-client-ui-settings-models'
  disabled: true

- insert:
    - id: multikey-provider
      name: dsh-multikey-provider
