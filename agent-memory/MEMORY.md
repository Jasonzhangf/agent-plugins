# dsh-memory project memory

- 2026-09-02: Jason requires every eligible tool-call and compaction-summary output to carry an index-ready `memory` field. Valid emissions enter a durable Pending Index immediately, but long-term Knowledge receives them only through successful automatic or manual memory compaction. Compaction appends both raw entries and organization deltas; capacity compaction replaces the oldest 10% of the active index projection with child-referenced compressed segments. Incremental and full organization commands share the same transaction owner. Tags: #memory-emission #pending-index #compaction #organization-delta #ten-percent
- 2026-09-02: Delivery order is fixed: root main governance → source pin → high-level design → architecture zones → one independently verified worktree per foundation capability → foundation module → DSH plugin → profile wiring → deployed verification/review. Root main is inspection/integration only; unverified capabilities cannot enter the foundation module. Tags: #root-main #worktree #foundation-capability #plugin-wiring
- 2026-09-02: dsh-memory is a plugin subproject at `dsh-plugins/dsh-memory/`, not an independent Git repository. Git root and root `main` belong to `dsh-plugins/`; physical worktrees live under `dsh-plugins/playground/`. Tags: #dsh-plugins #repository-root #no-nested-git #worktree
- 2026-09-02: Jason уточнил memory admission: prompt строго требует `memory`, но отсутствие или плохая schema никогда не отклоняет parent output и не вызывает retry; tool-call, assistant end-turn и compaction summary при наличии可识别 entries 都进入收集。手工、定时与 compaction organization 共用 transaction owner。存储需支持独立 root、只读备份、format version 与显式迁移。 Tags: #soft-admission #end-turn-memory #scheduled-organization #backup #migration
- 2026-09-02: Detailed design approved: three source observers feed one soft-admission contract and one Pending generation; manual, scheduled, capacity and context organization share one transaction owner; root/backup/version/migration are explicit store responsibilities. Tags: #detailed-design #single-transaction-owner #versioned-store
- 2026-09-03: Git main protection is enabled in the clean memory and TUI owner worktrees; `appsdk verify-git-main-protection` passed for both project roots. Tags: #git-main-protection #hooks #owner-worktree
- 2026-09-03: RCC `http://127.0.0.1:4444/v1` is reachable and authenticated chat completions succeed for the configured route; empty `/models` is a catalog limitation, not provider unavailability. Structured organization JSON also succeeds. Tags: #rcc #provider #organized-index #online-evidence
# Verified foundation: versioned persistence

- `47a9e09` adds `StoreManifest`/`VersionedStore` with `dsh.memory.store.v1`, user-selected root layout, atomic manifest writes, immutable backup copy, staged migration, and explicit integrity/version/backup errors.
- Migration verifies source hashes before backup and staged hashes before activation; failure leaves active manifest bytes unchanged. Root gates: `cargo test --all-targets` (18 passed), `cargo fmt -- --check`, `cargo clippy --all-targets -- -D warnings`, `appsdk verify dsh-memory`.
- `a85e79f` adds `MemoryStateSnapshot` persistence at `index/state.json`; manifest hash binding makes tampering fail closed, and round-trip recovery is covered by two tests.
