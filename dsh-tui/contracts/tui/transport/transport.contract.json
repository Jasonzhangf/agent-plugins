{
  "schema_version": 1,
  "contract_id": "dsh-tui.transport",
  "owner": "dsh-tui::transport",
  "endpoint_precedence": ["cli", "env", "default"],
  "default_endpoint": "http://127.0.0.1:3080",
  "validation": [
    "absolute http origin only",
    "loopback hostname only",
    "no credentials, query, fragment or non-root pathname",
    "invalid input fails before terminal activation",
    "no alternate endpoint fallback after selection"
  ],
  "carrier": {
    "client_base": "alpha4 remote facade",
    "frame_schemas": "alpha4 remote.mux stream protocol",
    "protocol": "HTTP unary + one downlink WebSocket per event stream",
    "endpoint": "typed control resource; never enters Session request or presentation payload"
  }
}
